# MusicBuddy distribution

Public update channel for [MusicBuddy](https://github.com/bennett-ctrl/MusicBuddy) (private source). Holds the latest `MusicBuddy.zip` and `version.json` that the app checks for updates. Published via `release.sh` in the source repo.
